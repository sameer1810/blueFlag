import React from 'react';

const FooterQuote = () => {
  return (
    <div className="bg-black py-8 flex justify-center items-center">
      <blockquote className="text-white italic text-center max-w-xs sm:max-w-md md:max-w-2xl lg:max-w-4xl xl:max-w-6xl text-base sm:text-lg md:text-xl lg:text-2xl xl:text-3xl">
        “Strive for perfection in everything you do. Take the best that exists and make it better. When it does not exist, design it.”
      </blockquote>
    </div>
  );
};

export default FooterQuote;
